from django.apps import AppConfig


class ArmWellsConfig(AppConfig):
    """
    Note that this changes the import path in `INSTALLED_APPS`.
    """
    name = 'armstrong.core.arm_wells'
    verbose_name = "Wells"
